<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts por Categoría</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <%- include('fragments/css.ejs') %>
    <style>
        body {
            background-color: #f8f9fa;
            color: #343a40;
        }
        h1 {
            text-align: center;
            margin: 20px 0;
        }
        .card {
            max-width: 300px;
            max-height: 500px;
            overflow: hidden;
            margin: auto;
        }
        .card-img-top {
            max-width: 100%;
            max-height: 200px;
            object-fit: cover;
        }
        .card-body {
            height: 150px;
            overflow: hidden;
        }
        .card-footer {
            text-align: center;
        }
    </style>
</head>
<body>
    <%- include('fragments/navbar') %>
    <h1>Posts en la categoría: <%= category %></h1>
    <div class="container">
        <div class="row justify-content-center">
            <% if (posts.length > 0) { %>
                <% posts.forEach(post => { %>
                    <div class="col-md-4 mb-4 d-flex align-items-stretch">
                        <div class="card">
                            <img class="card-img-top" src="<%= post.imageUrl %>" alt="Imagen del post">
                            <div class="card-body">
                                <h5 class="card-title"><%= post.title %></h5>
                                <p class="card-text"><%= post.description %></p>
                                <a href="/post/<%= post._id %>" class="btn btn-primary">Leer más</a>
                            </div>
                            <div class="card-footer text-muted">
                                <%= new Date(post.date).toDateString() %>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No hay posts en esta categoría.</p>
            <% } %>
        </div>
    </div>
    <%- include('fragments/footer') %>
</body>
</html>
